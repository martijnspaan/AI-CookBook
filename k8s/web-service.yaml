apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: ai-cookbook
  labels:
    app: web
spec:
  selector:
    app: web
  ports:
  - name: http
    port: 4200
    targetPort: 4200
    protocol: TCP
  type: ClusterIP
